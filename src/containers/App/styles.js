const styles = () => ({
    box: {
        display: "flex",
        alignItems: "center"
    },
    shape: {
        padding: 20,
        margin: 10,
        background: "red",
    }
})

export default styles;